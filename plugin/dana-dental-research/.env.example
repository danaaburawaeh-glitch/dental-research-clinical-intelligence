# DANA Dental Research & Clinical Intelligence — v0.4 connector configuration
# Placeholders only. Never commit real values into this file or anywhere in plugin source.
# Copy to your own untracked .env (or set these as real environment variables) to configure
# a running deployment. See docs/CONNECTOR_SECURITY.md.

NCBI_API_KEY=
NCBI_TOOL=dana_dental_evidence
NCBI_EMAIL=
CROSSREF_MAILTO=
